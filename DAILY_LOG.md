# Daily Log

Newest entry at the top. One entry per day, added by whoever (or whichever LLM session) did the work.

---

## 2026-08-13
Built a **Project Coordinator onboarding mind map** for Sreejith (the new hire), grounded in his JD, the WDCO PC Manual, and how K5M actually runs — published as an Artifact, corrected through several rounds of real-name/role fixes (Abdellatif Menbar is the main client rep for the whole project, not just FFE/compliance; Shagun's own reporting line is Claire + Khamille only, Khamille being WDCO's Associate Director, not an H&T contact), plus an Umbrella-Task-ownership section and a first-month ramp section sourced from `WC_PC_Onboarding Checklist Form.pdf`. Exported twice as PDF — a simple screen export, then a true single-page A3-landscape printable version, which took real print-CSS iteration (the key gotcha: flex/grid containers don't split across a print page break, a block that doesn't fully fit jumps whole to the next page) — prompted **Hard Rule 21**: always ask "screen export or printable?" before building a requested PDF, never guess.

Created branch **`007_wdco-internal-team-management`** to hold the onboarding materials with real names in them, which surfaced a genuine Hard Rule 1 conflict — resolved with a new, lighter exception mechanism (documented in `SECURITY_PROTOCOL.md`): any document marked **`CONFIDENTIAL — Internal Use Only`** at the top may contain real names, no codeword needed, the marking itself is the signal. Added `260813_WC_PC_Onboarding_Checklist.md` and `260813_K5M_PC_Coordination_Map.md` (both real-names, CONFIDENTIAL-marked) to that branch. `COMMAND_CENTER.md`'s `#GoodMorning` now also gives a standing reminder to check Sreejith's onboarding checklist daily (reminder only, not a status readout).

Studied `3D-Task Connected.pptx` (a task-visualization deck Shagun started, tying the Fitout Scope to the 3D reference render and material photos) per her "study it, ask me questions" instruction — read-only against the live Fitout Scope Excel throughout, never requiring her to close it. Built slide 8 (task A/3/2, stone arch to the bedroom entryway) as a one-task-at-a-time proof, precisely measuring the existing slides' connector geometry via a percentage-grid overlay before placing the new one, rather than eyeballing it — delivered as a review copy, confirmed correct. Shagun then chose to finish the rest of the deck herself after seeing the per-task time estimate (~90-140 min for the 5 remaining Entrance tasks).

Ran the **three-source Fire & Life Safety compliance framework** (Morocco's `Code local Maroc.pdf`, international hospitality standards, Hyatt's Supplier Code of Conduct) through to a full item-level build: confirmed the Hyatt SCC has zero technical fire/material content (pure business-ethics/labor document — dropped from the technical matrix); confirmed Morocco's own classification is the French-derived **Classement M (M0-M4)**, not Euroclass, with a real Annex 1 list of governing NM (Normes Marocaines) standards, several of which are direct ISO adoptions (NM ISO 6941 for textiles, 11925-2/-3 and 9239-1 for wall/floor reaction-to-fire, 3008 for fire doors, 12543 series for safety glass); confirmed via the code's own §2.6.5 that Park Hyatt Casablanca is **Type O** ("Hôtels et pensions de famille"), not the niche Type OA. Found (unverified — secondary source, not Hyatt's own site) that Hyatt International Hotels may contractually require NFPA 101 as a baseline for new-build properties regardless of local code — flagged as needing confirmation against an actual Hyatt/Park Hyatt technical standards document, not yet found in the project files. Delivered `260813_K5M_Fire-Life-Safety_Compliance_Matrix.xlsx` (Desktop) — Standards Reference + 26-row FFE Items + 38-row Fitout Items, validated (clean COM open, zip-integrity check) before delivery. Real gaps surfaced: no Moroccan or international-clear upholstery-ignitability standard confirmed for the two stools/pouf/armchair/banquette (falls back to BS 5852 Crib 5 by default, not a verified local requirement); open question on whether built-in joinery (wardrobe/luggage rack/bar/closet/vanity box) counts as a fixed wall finish or freestanding furniture for compliance purposes; open question on whether any of the three doors (A/7, A/8, B/14) actually need a fire rating. Logged as **Task Number 57**, due 18 Aug, Umbrella Task Compliance Collection.

Ran `#totalcheck` end of day — see that entry for the full close-out.

## 2026-08-12
Resolved the embedded-images question left open from 11 Aug: given the openpyxl full-sheet rebuild cannot preserve the Fitout Scope sheet's 60 "Place in Cell" reference images regardless of approach, Shagun chose to proceed with the rebuild and re-insert images herself afterward, rather than the more complex targeted-XML-preservation alternative. Applied the validated Entrance restructuring (A/3 split into A/3/1/2/3, FFE/2 relocation, A/2/A/4 Column N codes) to the real Desktop file; generated an old-row→new-row image-placement reference table so Shagun knows exactly where each of the 60 images needs to go back.

**Built out Column I/J (Ariel plan/page) and L (material code) for the entire Entrance area**, interactively — Shagun corrected several drawing-number/page assumptions directly against the Ariel PDF (the A/3 arch splits map to the "ARCHITRAVE" detail pages 33-35, not the SDB-312 pages I'd first guessed; FFE/2's three sub-items each map to distinct elevations on Ariel page 11, cross-checked material-by-material against the actual page text; A/8's one-material-per-row structure preserved exactly as already formatted). Surfaced and added a genuinely new scope item, **A/9** (wall paneling, wood A-WD-02c, wallpaper excluded), discovered via the Ariel page-11 elevation while resolving a "double portes pivot" ambiguity between the bathroom-entrance door and the mini-bar/wardrobe doors.

**Ran the same DD-100-vs-scope completeness audit for Bathroom** (14 BOQ items, B/1-B/14): flagged and resolved 3 material-code mismatches (B/3 baseboard — kept scope's stone over DD-100's wood, flagged as a likely drafting error per the 3D reference, to raise with Adil; B/5 threshold joint code A-ME-01a→A-ME-02h, plus traced a stray "(Porte serviette)" annotation to the actual Vasque/basin detail sheet and relocated the note to B/10; B/12 niche A-ST-07a→A-ST-01c per 5 consistent DD-100 references, plus added a previously-unlisted glass shelf), added new item **B/15** (pendant-light wall bracket, metal A-ME-01a, qty 2), and relocated a misplaced item — **FFE/4** ("vanity box," WDCO code already carried the SDB prefix) — out of the Bedroom section and into Bathroom, directly after B/11. Full I/J pass done strictly from the Ariel file only, per Shagun's explicit rule never to fall back to the client DD-100 directly — several items (B/2, B/4(a), B/4(b), B/5) came back "Awaiting correct files" since Ariel's redraw never included the SDB-100/200 overview and elevation series at all, only the SDB-3xx detail sheets. Full L pass done for all 16 rows.

**Same audit for Bedroom** (C/1-C/8, FFE/1, FFE/3): corrected C/1's floor code (A-WD-04d→A-WD-04b) and fixed a genuine stray-placeholder bug in C/3 — the literal word "Hello" sitting where a joint material code belonged, corrected to A-ME-02h (matching DD-100). Added new item **C/8** (window treatments — roller + venetian blind — left deliberately minimal, an internal RFQ is still open). Relocated **FFE/3** (mirror inside the arch) to sit directly under C/7 per Shagun's explanation of its history — the horizontal mirror piece was originally in the BOQ, got missed, shuffled to FFE scope, then moved back to fitout post-award — recorded as a combined entry rather than merged outright, final treatment deferred. Filled in **FFE/1**'s (fitted banquette) previously-empty material references with the structural wood code (A-WD-02c, confirmed by Shagun) and three fabric codes Shagun supplied directly from the material list (FA-04 main fabric, FA-05/FA-08 decorative cushions). I/J pass done at page-level precision — Shagun corrected several of the drawing-number-range guesses down to exact confirmed pages, and flagged one expected detail sheet (an analogue of the numbered "-313"-style threshold detail used elsewhere) as genuinely missing from Ariel's set despite a direct look.

**Separately, built the confidentiality-clean, bilingual-then-trilingual 3D reference file** (`260811_K5M_Refrence 3D.pdf`, 24 pages) — Output 2 from the original 3-output task list. Redacted: the real client name/location from a repeating footer line, the Gilles & Boissier logo (an embedded image, not text — required a different redaction method than the text-based footer fix), a location-identifying "Vue sur la Place des Nations" callout, and the "PHC" item-code prefix (replaced with "K5M", matching the existing WDCO code convention) — verified zero confidential-term hits across all 24 pages after rebuild, not just spot-checked. Added English translations below every French label, with a collision-avoidance system (tries below the original first, falls back to the right if that would overlap something) after an initial pass silently dropped ~30% of translations in tightly-packed furniture-legend lists. Added a third Simplified Chinese layer on request (own draft translation, explicitly flagged as pending review by the China office) in blue, using Microsoft YaHei (already present on this machine) via PyMuPDF's built-in CJK support — caught and fixed a real letter-spacing bug where Chinese characters rendered with zero tracking between glyphs, by inserting each character individually with an added gap. Final file: `260811_K5M_Refrence 3D_SUPPLIER READY_v2_ZH.pdf`.

Throughout the day: repeatedly found the real Fitout Scope file open in Excel (Shagun reviewing) when attempting a write — never forced an overwrite, waited for confirmation it was closed each time before proceeding, consistent with Hard Rule 20's spirit even for a Desktop-only file.

Ran `#totalcheck` at end of day — see that entry for the full close-out.

## 2026-08-11
Formalized **Hard Rule 20** (never write directly to a OneDrive-synced file — copy to Desktop, work there, Shagun moves the final version to OneDrive herself) and added **`WDCO_MICROSOFT365_NOTES.md`** (main branch) — a technical reference on WDCO's Teams/OneDrive-based Microsoft 365 environment: OneDrive file-lock COM failures, in-cell "Place in Cell" images (traced via `metadata.xml`→`rdrichvalue.xml`→`richValueRel.xml`→media), clipboard Copy/PasteSpecial fragility, `GetActiveObject()` unreliability, PowerPoint COM quirks, and mail-connector specifics. Both cross-referenced from Hard Rules 15/20.

Built the **K5M — H&T Delays Report** (PDF, branded) for management: shop-drawing review feedback still incomplete against the 20 Jul deadline (only 2 of several products addressed by 27 Jul), no 48-hour acknowledgment on the process-of-work or wooden-plank technical-data-sheet asks despite that being agreed in a meeting, and the Material Package's own deadline slipping from 8 Aug to a newly-requested 16 Aug — framed as a clear 8-day delay against H&T's own revised date.

Added **Umbrella Task 16, "Site Management"** (on-the-ground execution coordination didn't fit any existing pre-site-planning category) and **Task Tracker #58**, "Find and vet a local installer for on-site work (Morocco)" (due 30 Aug, priority important-not-urgent).

Opened real work on the **K5M Fitout Scope** (`260811_K5M_FitOut_COC.xlsx`, sheet renamed "Fitout Scope" by Shagun), toward three outputs: a finalized Scope of Work with drawing/page references, a confidentiality-clean 3D reference file, and a DD-100↔Ariel's-resized-set cross-reference table. Built the **Ariel Page Index** (96 rows, cross-referencing Ariel's 89-page A4 redrawn set against the original DD-100) — resolved a real drawing-number collision (SDB-307/308/309, traced to the client's own original DD-100, not introduced in the redraw) and the "T2B2 vs T1B1" question (an intentional architect cross-reference, "IDEM T1B1" — resolved by using the source filename `ATT00001` as the reference number rather than forcing a renumber that would've collided with existing T1B1 content).

Ran the **DD-100-vs-Fitout-Scope audit, Entrance area** (per Shagun's explicit area-by-area sequencing — Entrance, then Bathroom, then Bedroom, excluding wall paint/wallpaper/FFE from gap-flagging): surfaced 5 possible gaps, 1 open MEP policy question, and 2-3 material-code mismatches. All resolved directly with Shagun: split the A/3 stone-arch line into A/3/1 (bathroom door, 5.5ML) / A/3/2 (bedroom entryway, 5.5ML) / A/3/3 (wardrobe/mini-bar/luggage rack, 10ML incl. a 0.7ML Resti-reconciliation margin) summing back to the original 21ML BOQ; relocate FFE/2 (Wardrobe/Luggage Rack/Bar Cabinet) to sit directly after the new arch blocks; use the sheet's new Column N to record DD-100-verified material codes for A/2 and A/4; MEP confirmed fully out of scope for all future area audits.

Began executing the Entrance restructuring against a scratch copy only (never the real file — Hard Rule 17/20). Caught two real bugs in the rebuild script before running it against anything live: a `MAX_COL=14` cap that would have silently truncated every row by two full columns (the sheet actually runs to column P — both O "Type of Work" and P "Reference Image" are real content, not headroom), and a hand-typed merge-column list that didn't match the sheet's actual merge ranges (verified directly, not assumed). While validating the corrected script, discovered the sheet's 60 embedded reference images are stored as in-cell "Place in Cell" rich-value images (via `drawing1.xml` + `richValueRel.xml`) — a format openpyxl's own writer cannot round-trip, so *any* save through it, this rebuild or otherwise, would silently wipe all 60 images sheet-wide, not just the rows being restructured. Paused before touching the real Desktop file to flag this. Started a `#totalcheck` at end of day but it was interrupted mid-way (`#STOP`) when Shagun redirected to a fresh task list for the Fitout Scope work — carried forward and completed properly on 12 Aug instead (see that entry).

## 2026-08-10
Built the **T1B1 Hard Deadlines Schedule** — backward-planned from the client-agreed 20 Dec 2026 site-ready date (confirmed via a mailbox search on "project schedule" after several failed subject-line-based attempts) — as a branded HTML→PDF evidence sheet, M1-M8 milestones, saved to a new dedicated OneDrive folder (`01_INTERNAL\Overall Project Management\001_Schedule`). Iterated once more to drop a weekends/holidays assumption per review.

Built the **Material Package Photo Presentation** (47 slides) from the FFE COC and both Fitout Excel workbooks, through a strict iterative build-review-fix cycle. Caught and fixed a real thumbnail-collision bug: the first three FFE Fabrics slides were showing Fitout Fabrics images instead, traced to a shared filename-prefix pattern across two separate extraction passes plus a folder merge that overwrote the correct files — fixed by re-extracting under a distinct prefix and rebuilding. Extensive Microsoft 365/OneDrive COM automation troubleshooting along the way: 4 consecutive automation failures against the live OneDrive Fitout Material Package (each leaving an orphaned Excel process), resolved pragmatically by working from a Desktop copy instead — this is what led directly to **Hard Rule 20** (formalized the next day). Deployed the finished deck to both Desktop and `03_TECHNICAL\003_Materials\`; two further surgical fixes (28 "SAMPLE SIZE" notes added by category, the 3 wrong FFE-Fabrics thumbnails corrected) were applied directly to the OneDrive copy afterward, each time only after Shagun's explicit confirmation it was safe.

## 2026-08-07
**Merged the Task Tracker and Project Calendar** into one workbook, `260807_K5M_Project Management_Tasks and Calendar.xlsx` (`006_task-management-pm`) — the build agreed in principle back on 4 Aug. Renamed "S.No." to "Task Number" (Hard Rule 8 updated) so it can double as the connecting key: calendar entries get tagged `(#N)` with a `HYPERLINK()` back to the Task sheet row, and a new `Logged on Calendar?` column on the Task sheet checks the relevant month tab for that tag via `COUNTIF`+`INDIRECT` (deliberately not `FILTER()`, to avoid an Excel-version dependency) and reports Yes/Missing/N/A — verified computing correctly. Built via COM `Sheets.Copy` to preserve all formatting/validation natively rather than reconstructing it in openpyxl. Old separate Task Tracker and Calendar files marked superseded, not deleted. `COMMAND_CENTER.md` updated throughout to reference the merged file.

**Full read-only study of the entire K5M OneDrive folder** (~700-800 files across 8 top-level areas), at Shagun's request, using 5 parallel read-only research agents (one per major area) — confirmed real project identity (Park Hyatt Casablanca, Interdec Maroc SA, Gilles & Boissier, Wood Couture as FF&E+Fit-out contractor), full commercial picture (~$261,550 awarded T1B1+T2B2 package, still provisional), and a consistent pattern of real but non-catastrophic issues: 3-4 parallel item/project-coding schemes in live use, broken formulas in two live commercial/technical sheets, heavy file duplication (~1GB in `02_COMMERCIAL` alone), trackers stuck at "To Be Submitted" status despite real progress happening only in remarks columns, and several date-digit-transposition folder-naming slips that created empty duplicate folders independently in 3+ places.

Built a **three-part standing system** from that study: a generic, redacted "OneDrive Project Folder Structure" template (`005_project-discovery`, git — reusable for future projects, includes a mermaid diagram and the naming-convention lessons above); a real, un-redacted `_K5M_Folder_Guide.md` and `_K5M_File_Index.xlsx` (707 files, tagged by Umbrella Task at folder level) at the OneDrive K5M root; and a new on-demand command, `#checkonedriveproject` (never automatic, Shagun's own roughly-weekly cadence), that diffs against the saved index and re-audits only what's new or changed. Added a 15th Umbrella Task, "Contractual - Client Facing," to cover `02_COMMERCIAL` (BOQs/pricing/proposals), which didn't fit any existing category.

Then reversed the folder guide's location on Shagun's instruction: real-names content isn't allowed to sit on the open OneDrive after all — `_K5M_Folder_Guide.md` was removed from OneDrive and rebuilt as a git-only file (main branch), as an **explicit, named, codeword-gated exception** to `SECURITY_PROTOCOL.md`'s no-real-names rule. The codeword ("Mad Max Fury Road," Shagun's choice) and the gating rule are both documented directly in `SECURITY_PROTOCOL.md` itself so a future session without this conversation's context still respects it.

**New canonical COC location established**: `03_TECHNICAL/001_COC/260804_K5M_FFE_COC.xlsx` (OneDrive) — Shagun's own direct continuation of the Central COC build studied 6 Aug, now the one place FFE materials and SHD get updated; client-facing files are to be updated *from* this file, not the reverse (Hard Rule 16, now anchored to a specific file). Studied read-only, no changes, per explicit instruction: healthy, 5 sheets, real progress since 6 Aug — the SHD tracker now has 68 remarks (up from 63) with 136 of 288 Action/Owner cells filled from an internal review meeting held 5 Aug, resolving what had been an open content gap (Task Tracker S.No.44's MOM). SHD01/SHD02 still untouched; all Values still False.

**Outlook/MCP access — resolved.** The `outlook-email-drafter` connector stayed blocked at the tenant level even after Shagun got WDCO IT's sign-off (the fix hadn't propagated to that specific app registration). The claude.ai **Microsoft 365** connector, authorized separately via `/mcp`, worked immediately — full read access, no tenant block. Confirmed identity (Shagun Gupta, Project Manager, shagun@woodcouture.com) and ran a first live search — surfaced several real, unlogged items on the spot (a high-importance schedule-risk email from Khamille, Ariel's fitout-drawing delivery, the Maria/container email).

**Full read-only mailbox study**, at Shagun's request, to refresh project understanding — three parallel agents split by date range (before 1 Aug / 1-4 Aug / 5-7 Aug), each paginating fully through all "K5M"-tagged mail and reading full bodies on substantive threads, strictly read-only throughout (no send/draft/delete tools ever used). ~200 emails read in total. Key findings: the real origin story of the stone-arch BOQ gap (a 28 Jul scope clarification, not a data error); a genuine date discrepancy — the actual **contractual** mock-up deadline is **30 November 2026** (per the original award letter, cited by Filippo Sona in a live liability-negotiation email), while the Hard Milestones list's "20 Dec" figure is a later, internal revision that has drifted *past* the real contractual date, undetected until now; a new unlogged schedule risk (Khamille, high-importance, 7 Aug: K5M factory materials now only completing 16 Aug, compounded by H&T capacity being split with another WDCO project, "2576-F"); and confirmation that three previously-open Task Tracker items were actually already resolved via email (BOQ email to Filippo sent 6 Aug, Ariel's drawings delivered 7 Aug, container email to Maria Al Balushi sent 7 Aug with real numbers). Also surfaced full identities for several previously-undocumented names ("Arlene" → Arlene Liang; "Maria" → Maria Al Balushi; H&T's actual primary contact, Alfred Li) and a wood/stone material change (Onyx → Taj Mahal) driven by Gilles & Boissier.

Built this week's project report (`K5M_Weekly_Report_07.08.2026_DRAFT.docx`, matching the established 8-section email format) with the full stone-arch BOQ reconciliation table embedded, plus a new **Umbrella Task Status Chart** (`260807_K5M_Umbrella Task Status Chart.xlsx`) — all 15 Umbrella Tasks as rows, styled to match Shagun's own preferred WDCO/Production Partner/Client-side status-matrix format (green/amber/red, deadline highlighting) from an existing reference file.

Updated the merged Task Tracker + Calendar workbook end-to-end from the mailbox findings: closed 3 tasks, added 9 new ones (including reconciling the 30 Nov contractual-date gap, chasing H&T's 8 Aug sample-box deadline, and tracking the H&T capacity risk), and did a further Woody pass catching 3 more status updates the emails revealed (H&T's metal samples did start but came back the wrong size; Ariel has been actively working, not just asked; the SHD01 MOM was in fact sent, one day late). Hard Milestones updated with the real 30 Nov contractual date, the 10 Oct container hard deadline, and the 16 Aug factory-risk flag — each tagged back to its Task Number.

---

## 2026-08-06
Iterated the stone arch BOQ reconciliation table (built 4 Aug) through several real rounds with Resti's direct input: he confirmed door-jamb depth doesn't affect the calculation method (so 6&7 can be treated the same way as 1,2,3&4), identified that the client built margin into the BOQ for 6&7, and suggested mirroring that margin into 1&2 (5.3→5.5 ML) — tightening the entrance-section reconciliation to ~3.3% under BOQ. Ended with all 8 arches resolved except one genuine open item: a second physical banquette arch confirmed to exist but not covered by any BOQ line. Rebuilt the table three times on layout feedback alone (wide scrolling table → responsive card layout → back to a real table fixed to A4 landscape width) before landing on something that actually displays without cutoff.

Studied (read-only, no changes per explicit instruction) `260804_K5M_Internal Central COC.xlsx` — Shagun's own in-progress build on Desktop, not yet in git. Confirmed healthy (clean zip check, opens without a repair prompt). Five sheets: FFE_Main Data Sheet (exact match to canonical scope), Change Log, FFE_Fabrics and FFE_Hard Material (evolved Material Package structure with real per-material Client/Supplier/China Office Remarks/Translation threads), and a Shop Drawing Tracker built from the structure designed with Claude on 4 Aug — now populated with real SHD00 feedback for all 17 FFE items (63 remarks, all still unresolved, 44 already have an Action/Action Owner noted). SHD01/SHD02 still empty. This is visibly the COC rebuild agreed 3 Aug, now well underway.

Opened a discussion (not yet resolved) on restructuring the Material Package into three trackers — Main Central (confirmed to mean the COC itself), Client-facing, and Supplier-facing — same column format across all three, diverging only where sensitive information starts. Found the two SharePoint locations Shagun linked map to real OneDrive folders already in active use, including a previously-unlogged `260805_K5M_Tracker Updates Arlene` folder (unprotected copies with Shagun's own remarks) — confirmed Arlene is a real, recurring party in the fabric/hard-material feedback loop via the Central COC's remark columns.

Added **`#STOP`** to the Command Center — an immediate hard interrupt, distinct from a plain "wait."

Designed a new mail-monitoring system: `#GoodMorning` (extended) and a new `#checkmail` command will check Outlook for anything tagged/subject-prefixed "K5M," log it (sender/subject/date/summary/matched Umbrella Task) to a new `004_communication/K5M_Mail_Log.md`, and flag anything described in an email that isn't yet reflected in the Task Tracker or Dev Log. Logged as a `#newtask` (installing the MCP access), then found a **hard blocker** testing it: the `outlook-email-drafter` connector authenticates fine but Wood Couture's Microsoft 365 tenant has an Application Access Policy blocking this class of app from reading any mailbox — needs IT/admin action, not a personal authorization step.

Ran `#totalcheck`: marked S.No.14 and S.No.38 Done (SHD00 collation, Resti's arch calc write-up), marked S.No.5 Stale as a duplicate of S.No.31 (both Ariel fit-out-drawings asks), caught and fixed an invalid "Blocked" status value that shouldn't have been possible — which surfaced a real gap: the Status/Umbrella Task/Priority/Type dropdown validation only covers rows 11-33, so every row added since (34 onward) has no validation at all. Logged that as its own low-priority task rather than fixing it mid-check.

---

## 2026-08-04
Distributed a full set of meeting notes from a meeting with Claire into the project's systems (contacts, Task Tracker, Calendar, new meeting folder) after a clarifying-questions pass, then executed on `#execute`. Introduced Resti (new Head of Engineering) and Tharusha (draftsman) to `CONTACT_SHEET.md`. Extended the site visit to a 2-day effort (31 Aug–1 Sep) across the Calendar's Hard Milestones and mini-calendar.

Recovered `K5M_AI_Assisted_Project_Management.pptx` from the Windows Recycle Bin after the user recalled deleting it — `InvokeVerb("restore")` failed silently, but copying directly from the underlying `$Recycle.Bin` GUID path worked. Verified all 10 slides intact.

Closed out the stone arch BOQ scope-gap investigation (started 3 Aug): read all 69 DD-100 pages directly, corrected an initial 8-arch count down to 7 (two callouts were the same physical arch), measured each arch's linear-meterage from the drawings, and reconciled against the Sales Order/BOQ arch-by-arch. Final 7-row table with page numbers, BOQ/drawing cross-references, and reconciliation notes saved to `006_task-management-pm/meetings/260803_K5M_Meeting_BOQ_Scope_Gap/post-meeting/Fit-out BOQ Scope Gap/`. One genuine unresolved discrepancy flagged for the supplier/design team: the shower arch's BOQ quantity (21 ML) is ~4.3× the measured single instance (4.88 ML), plus a material mismatch (drawn as A-ST-01c, priced as A-ST-07a).

**Built the Shop Drawing Tracker from scratch** (`003_trackers/shop_drawing_tracker/260804_K5M_Shop Drawing Tracker.xlsx`), through a discussion-only design phase (per Hard Rule 13) before `#execute`. Two sheets: **Data** (exact copy of the canonical FFE Master SKU List, all 27 embedded reference images preserved) and **Shop Drawing Tracker** (WDCO Item Code linked to Data, three SHD rounds — SHD00/SHD01 with collapsible per-stage remarks tracking Client Feedback/Internal Review/Factory-Production Partner Review, each gated by a TRUE/FALSE/Not Going Forward dropdown; SHD02 with no remarks at all by design — just a readiness check plus a four-item GFP checklist — Compliances, Material Swatches, Pictorial Reference, No Handwritten Notes — before the client-signature hyperlink). Went through three revisions same day as the user worked in it live: switched the Item Code link from a fragile row-position formula to a robust `INDEX/MATCH` keyed on Data's "No." column (needed after she deleted two no-code rows, which broke the simple positional link), added a Product Picture Reference column with compressed thumbnails (kept file growth modest — 7.7MB → 9.1MB, deliberately not reusing full-resolution originals), and mirrored the user's own SHD00 restructuring (a "Created On" date and a "Ref No." column per remark stage) onto SHD01 for consistency. Caught and fixed a recurring bug from Excel's column-insert behavior: the Value dropdown validation kept auto-expanding into newly-inserted adjacent columns each time a column was inserted next to it. Checked (but did not adopt) an older reference file's SHD tab (`Ultimate COC Copy.xlsx`, `001_inbox` branch) — a rolling dated-status-letter log rather than a remarks-tracking design; confirmed by the user as the wrong fit here. Also noted the user has her own pre-existing `261607_K5M_FF&E_SHD_Internal Remarks Tracker.xlsx` on SharePoint — confirmed as unrelated, not something to reconcile with this build.

Ran `#totalcheck`: confirmed all trackers reflect the latest state and everything is pushed (no branch has local-only changes); confirmed Umbrella Task tagging is clean across the Task Tracker (no abbreviations found this time); confirmed previously-Stale rows are correctly hidden. **`#projectdevelopment` logging is explicitly pending** — no Project Development Log entries since 31 Jul; user flagged this herself and deferred it to be done together the next session, rather than rushed at end of day.

---

## 2026-08-03
Reviewed the FFE COC's status at Shagun's request — untouched since its 28 July v1 build, with a full round of 28 July feedback never applied (per the standing "wait for explicit direction" rule). Began applying it: removed freeze panes across all sheets, rebuilt the Data tab from the canonical FFE Master SKU List (Hard Rule 14), and started updating the Hard Material sheet (formatting lifted from a client-facing file Shagun preferred, High-Res Link transfer, new meeting-notes column).

**Caused and then diagnosed a real corruption incident:** partway through, the COC began throwing an Excel "problem with content" repair prompt. Traced likely cause to the column-grouping/outline feature added to the Hard Material sheet. Restored the clean 28 July version from git and redid the same edits without that feature — but COM automation still couldn't open the file at all, even in its pristine, untouched state, which showed the automation failure was a red herring (likely a size/memory limitation with this heavily-image-embedded 36MB file) rather than proof of what broke it. Shagun ultimately decided the COC should be rebuilt from scratch once the underlying data is fully sorted — that rebuild is still pending.

Reviewed the live supplier-facing FFE Material Package after Shagun's own edits: a new "Client Communication Update" column with per-material client feedback, and High-Res Link values added for four materials (though their Y/N flag hadn't been flipped — fixed). Found three of the new thumbnail images use Excel's newer native "Image in Cell" feature, which the project's Python tooling (openpyxl) can't read or move — agreed Shagun will copy those across to the COC manually rather than risk it programmatically.

**Caught a second real, longstanding bug:** the K5M Task Tracker's column widths had never been correctly realigned since the S.No. column was inserted days ago — widths stayed attached to their old column letters while the headers shifted right, so Task/Umbrella Task/Note (meant to be wide) were badly cramped while Date Raised/Due Date (meant to be narrow) were oversized. This had been sitting wrong since the original insert and wasn't caught by any of the visual checks done since. Shagun caught it by eye; fixed and verified via PDF render, then pushed to git.

## 2026-07-31
Studied both live OneDrive Material Package trackers (FFE and Fit-out, supplier-facing) at Shagun's direction. Found the FFE tracker's product codes didn't match the canonical scope — traced and confirmed four invalid codes (`CHA-RT-01`, `CHA-BT-01`, `CHA-SL-01`, `SDB-PF-01`), each resolvable with high confidence via the material cross-reference doc, pending Shagun's design-team clarification email before correcting them. Confirmed `260724_WDCOK5M_FFE_Master SKU List.xlsx` as the canonical FFE scope source (Hard Rule 14) and corrected its own `CH`→`CHA` naming lag relative to the COC. Fit-out scope remains unconfirmed, expected next week — reviewed its Material Package too and found it structurally messier (drawing-location task codes instead of item codes, several self-documented open conflicts), but per Shagun's call, parked the scope/localization columns rather than block on them.

Added meeting-based development notes (dictated by Shagun, from today's H&T material package review) to both Material Package trackers' new "Based on Meeting" column — values only, formatting untouched.

Built a Wood Couture-branded stone reference-image presentation (Alabaster, Calacatta Oro, Taj Mahal) for a design-team email, using the real brand template (logo, Playfair Display/Lato, color palette) since this is an outward-facing deliverable, not an internal tracker. Shagun added her own letter-badge labels (A/B/C...) to one slide; replicated the same treatment on the other two.

Introduced two new standing mechanisms at Shagun's direction: **`#hardrule`** (adds an entry to `HARD_RULES.md`, same pattern as `#newtask`) and **`#execute`** (the explicit go-ahead to start building anything — Hard Rule 13: never build directly, always confirm alignment and wait for this first). Also caught and fixed a second occurrence of the Excel-COM-closing-her-window bug, this time via `Dispatch` attaching to her already-open session rather than force-kill — fixed by switching all COM automation scripts to `DispatchEx` (Hard Rule 15).

Filed the H&T Material Package MOM into its own meeting folder (`meetings/260731_K5M_Meeting_H&T_Material_Package/`), separate from the Development Log narrative entries covering the same meeting. Drafted, then reviewed against git, this week's internal project report (sent by Shagun) — cross-checked every point against the Development Log and Task Tracker, closing three logging gaps (RFI closures with the design team, SHD V00 feedback received, Cathy Yang added to the contact sheet) and flagging two items still needing her clarification (an architect-side RFI closure, an unspecified open internal RFI).

Ran `#totalcheck`: corrected a due-date discrepancy on Task Tracker row 4 (was a month off — 5 Sep instead of 5 Aug), updated Fitout Fabrics to In Progress, added five new Hard Milestones for August (substructure list, Fitout SHD 01 start, sample box dispatch, SHD V01 to Paris, onsite survey), and fixed a second recurrence of the un-corrected Umbrella Task abbreviations on the Desktop Project Calendar copy (same fix as 30 Jul, on a file that had apparently reverted).

## 2026-07-30
Built the K5M Task Tracker (`006_task-management-pm/260730_K5M_Task Tracker.xlsx`) as a single continuous sheet replacing the old one-file-per-day task lists, and went through several rounds of structural revision at Shagun's direction: merged duplicate rows, removed the "Owner" column (Shagun is always default owner), split "Dependent On" into two columns, added a Stale status (hidden, not deleted, cleaned up by "Woody" — the name for Claude's tracker-maintenance role — during `#totalcheck`), added an S.No. column (Claude assigns sequential numbers, Shagun handles interdependency letter-suffix grouping herself), and priority color-coding matching the Priority Legend. Corrected an earlier misunderstanding: Umbrella Task values are always written in full everywhere — no abbreviations for the values themselves ("UTSK" is shorthand only for the term "Umbrella Task"). Built `COMMAND_CENTER.md` (main branch) documenting standing trigger phrases (`#newtask`, `#projectdevelopment`, `#totalcheck`, `#GoodMorning`, `#ShowMeMyKanban`).

Logged a BOQ scope-gap meeting for Monday (3 Aug) via `#projectdevelopment`, built a formal agenda for it, and reflected it into the Project Calendar's Development Log/Hard Milestones. Read the lighting designer email (confirmed existing Contact Sheet entry, no new contact needed) and logged its RFI implications.

**Caught and fixed a data-corruption incident:** a `#newtask` append script miscounted the last populated row (checking a column with legitimate blanks) and wrote a new task into an existing row, overwriting it. Found via post-write verification, restored the original row, and correctly re-appended the new task at the true end of the table. While verifying, also found the Status/Priority conditional-formatting ranges had gone stale (still pointing at pre-S.No.-insertion columns) — repaired both. Also established a new hard rule after `Stop-Process -Force` calls (intended to clear stray Excel COM instances) killed Shagun's own actively-open, unsaved Project Calendar window: never force-kill Excel/PowerPoint processes again — ask instead.

Logged five more `#projectdevelopment` updates (H&T Material Package follow-up/meeting, a design-team sample-photo request, SketchUp model work started, a basic BOQ agenda draft, and a fit-out drawings task paused pending a new team joinee) — updated the Task Tracker accordingly and caught two more leftover un-corrected Umbrella Task abbreviations in the Development Log from before the full-name fix.

Built `HARD_RULES.md` (main branch) — a single consolidated reference for every standing hard rule across the project. While building it, found the client's real name and "Wood Couture" written directly in `README.md`, `PROJECT_BRIEFING.md`, `SESSION_START.md`, and throughout `DAILY_LOG.md`'s historical entries — a pre-existing gap against the security protocol. Flagged to Shagun; she asked to hold off deciding scope for now. Logged as Task Tracker row 26 so it isn't lost across sessions.

Ran `#totalcheck`: reviewed the full Task Tracker, marked one row Stale (an H&T meeting-agenda task superseded by the meeting itself happening), confirmed all changes pushed to the correct branches.

## 2026-07-29
Backfilled the missing 2026-07-27 log entry (Material Package V1-V4 work had never been recorded here). Diagnosed two open COC questions from yesterday: confirmed openpyxl (and Excel's own COM/VBA object model) cannot write native checkboxes, so Shagun will insert them manually in Excel over the existing TRUE/FALSE cells; confirmed the Status auto-color conditional formatting rule is intact and renders correctly when tested directly — the issue Shagun saw was environment-specific, not a file bug. FFE COC itself was **not** rebuilt with the rest of yesterday's feedback (Master Scope column order, images, font size 9, collapsible remarks design) — still open, see `260729_Task_List.md`.

Built `CONTACT_SHEET.md` (main branch) — real names/roles for every external party (client contact, owner's rep, G&B design team, architect, lighting consultant) and WDCO's own team, each with the generic term to use instead in client/supplier-facing documents. Linked from `SECURITY_PROTOCOL.md`. Confirmed via full-repo search that no existing document had mislabeled the architect's input as "the design team."

Built the **Project Calendar** (`006_task-management-pm/260729_K5M_Project Calendar.xlsx`) from the client-facing and supplier-facing schedules — v1 (Data table, Jul-Dec 2026 month grids, Deadlines Summary, Development Log), then a substantial v2 rebuild after a full feedback round: recurring-task support (two rules — biweekly/weekly Friday updates, anchored 31 Jul 2026), 6-event-slot rows per day (was single wrapped-text cells), multi-day events now render as a merged tinted bar across their full date range (previously only shown on the start day), legend on every sheet, category color-coding without redundant bracket text, "Hard Milestones" tab rebuilt as a mini 6-month calendar grid with deadline days highlighted directly, and Development Log extended with Category/Umbrella Task/Evidence-hyperlink columns. Caught and fixed a row-height bug mid-build (fixed heights too short for wrapped text, causing visual overlap) via the same render-to-PDF-and-inspect discipline used elsewhere.

Introduced **Umbrella Tasks** — 12 ongoing project workstreams (Project Scope, Project Schedule, Compliance Collection, FFE SHD, Material Package FFE/Fitout, Specifications from client, RFI, Fitout SHD, COC Management, Communication Management, Site Survey Planning), plus a 13th added same day (WDCO Internal Project Management, `WIPM`) — documented in `006_task-management-pm/Umbrella_Tasks.md`, each with a short code (e.g. `RFI-C`, `MPFFE`) so Shagun can reference the code instead of the full name. `#newtask` and Development Log entries get tagged with one of these, in a batch during end-of-day `#totalcheck`, not live. A weekly report per umbrella task is planned (fresh dated file each week), not yet built.

Standardized the task-log trigger phrase as **`#newtask`** (previously used inconsistently as "new task" / "#newtask" / "#new task") — `COMMAND_CENTER.md` updated accordingly. Logged 7 `#projectdevelopment` entries covering: the Material Package email sent to G&B (FFE + Fitout), the Project Calendar reaching functional-complete, an RFI raised on GL-8's high-res photo, **the architect's DWG file for the entrance door finally received** (long-pending item — still needs studying and filing into the `001_inbox` archi package folder), a new lighting designer being introduced (contact details still needed to update `CONTACT_SHEET.md`), a meeting with Claire (MoM to follow), and fit-out RFIs being closed with the client informed. A few of these entries' Category/Umbrella tags were Claude's best guess, flagged for Shagun to correct.

Created `COMMAND_CENTER.md` (main branch) documenting the three standing trigger phrases — `new task`, `#projectdevelopment`, `#totalcheck` — and exactly what each does; linked from the README. Extended `#totalcheck` same day to include asking for unlogged project developments and end-of-day Umbrella Task tagging.

## 2026-07-28
Rebuilt and realigned the internal AI-usage presentation for the user's boss (10 slides — cover, why this session, the setup, the toolkit, Material Package case study, daily workflow, results so far, open questions, a single "looking ahead" slide, thank you); location TBD in this repo, pending the user's call on where it belongs.

Started the FFE/Fitout **COC** ("Center of Control") build — clarified this is a mature, company-wide WDCO practice (not something new to this project), reviewed a reference COC from another project plus K5M's own (mostly empty) existing SharePoint COC files, and agreed a strict build discipline: nothing gets built without explicit, tab-by-tab direction. Built v1 of the FFE COC (`003_trackers/internal_COC/260728_K5M_Internal COC_FFE.xlsx`) — Data (Master Scope), Hard Material, Fabrics, Shop Drawings, Change Log. A large feedback round came in the same day on Data/Hard Material/Fabrics/SHD, not yet incorporated — see `006_task-management-pm/260728_Task_List.md` for open items.

Added `005_project-discovery/Making_and_Maintaining_a_COC.md` — a genericized, reusable writeup of what was learned building this COC, meant to seed a proper COC-building SOP for future projects.

Introduced a new daily-task-list convention on `006_task-management-pm` (`YYMMDD_Task_List.md`) — the user will say "new task" going forward to add an item to today's list; the eventual goal is a real task-management system for this project built from these logs.

Still open from this morning, carried forward: PDF export of both Material Packages for the supplier folder, customer emails from 27 July, and a POC (point of contact) record — none of these have been actioned yet.

## 2026-07-27 (backfilled 2026-07-29 — no entry was made on the day itself)
Built V1 of both Material Package trackers — `003_trackers/material_package_ffe/260727_K5M_Material Package_FFE.xlsx` and `material_package_fitout/260727_K5M_Material Package_Fitout.xlsx` — then iterated through several same-day revisions based on direct user review. See each tracker's own `VERSION_LOG.md` for the full detail; summary below.

FFE went through V1 → V4: consolidated to one row per material (from one row per material-per-item-application), split into "Hard Materials" and "Fabrics" tabs in one workbook, fixed a font/row-height rendering bug and a thumbnail-embedding bug (13 of 15 images were silently failing — caught only by rendering to PDF and inspecting), removed unnecessary Finish columns, added a `PP-03` fabric row, and finished with a styling lock-in pass (Lato 11 only, solid black borders, anonymized — no client/design-team names in cell content).

Fit-out went through a similar V1 → V3 arc, plus one more serious bug: the Localization column's rich-text was rendering as a single stray character per row, because `InlineFont(sz=1000)` was read literally as a 1000pt font rather than "10.00pt" — fixed to `sz=11`. Also translated Localization fully to English, removed out-of-scope materials (paint/wallpaper/carpet), corrected several High-Res Available flags, and added a disclaimer that the FM-001–FM-012 codes used throughout are internal cross-references invented while reconciling BOQ/DD-100/material-list versions, not real RFI numbers. Finished with the same styling lock-in + anonymization pass, plus a new Fabrics tab for the banquette (Fit-out scope) fabrics.

Both files were then moved to OneDrive as the live, supplier-shared copies; formatting on both is now considered locked — further changes should be data-entry only unless the user explicitly reopens a structural decision. Agreed WDCO item-code scheme (`K5M-T1B1-[Area]-[Type]-[##]`) during this session, later corrected `CH` → `CHA` for Chamber on 2026-07-28.

## 2026-07-24 (end of session)
Added `002_knowledge-bank/shop_drawings/260601_DD100_T1B1_page_index.md` (full page-to-drawing-code index, date-locked so future DD100 revisions get their own file instead of overwriting this baseline) and `002_knowledge-bank/materials/T1B1_material_cross_reference.md` (material code → every location/page it's used, also baseline-locked with a changelog instead of in-place edits). Copied the actual first-received client package (BOQ, DD100 zip, Schematic Design PDF, FF&E item drawings zip) into `001_inbox/260724_T1B1_first_client_package/` — previously only reviewed from the user's Desktop, never actually preserved in the repo. Added `SESSION_START.md` to `main` with the exact prompt to paste at the start of any future session (this machine or another) to get fully caught up before doing anything else. User's plan: pick back up Monday with a project update; this repo should already be fully "git-ready" for that — nothing further needed before then.

## 2026-07-24
Repo structure designed and agreed: 7 branches (`main`, `001_inbox`, `002_knowledge-bank`, `003_trackers`, `004_communication`, `005_project-discovery`, `006_task-management-pm`). Folder numbering, tracker file-naming convention (`YYMMDD_ProjectCode_Category_SpecificName`), and the security protocol (project code only, "Production Partner" only, brand guidelines) were defined. Decided against migrating the old `building-COC-Version-00` branch content — clean start on this new repo. Scaffolding files (README, SECURITY_PROTOCOL, this log) created for the `main` branch.

Git installed and authenticated (via `gh`) so Claude manages the repo directly going forward — no manual uploads needed. All 7 branches created and pushed live to `github.com/ShagunWC/K5M`. Added `PROJECT_BRIEFING.md` to `main` as a standing catch-up document, since Claude Code's session resume is local to one machine and this project needs to survive a system change. Session ended with the BOQ about to be shared; goal is full project onboarding before any building resumes.

Reviewed, in full: the T1B1 BOQ (FF&E sheet $24,724.89 / 24 lines; Fit-out sheet $122,069.58), all architectural/FF&E/fabric/equipment/hardware material lists, the drawing index, the kick-off-meeting email thread (revealed real stakeholder map: client Park Hyatt Casablanca, designer Gilles & Boissier, owner's rep Interedec Maroc, architect Adil Mokaddem, Production Partner Wood Couture; mock-up deadline 30 Nov 2026), all 14 individual FF&E item drawings, the 57-page Schematic Design set, and the 69-page DD100 detailed drawing set (both via background research agents).

Key findings to act on:
- Bedroom furniture Options A/B (Schematic Design) share identical FF&E codes despite being physically different pieces (headboard, nightstand, console, bedside light). User confirmed **Option A matches the confirmed/priced FF&E** (table lamp, not pendant) — needs a real code fix before these can drive procurement.
- DD100 set has several drafting/QA issues worth raising with G&B: missing GEN-120/GEN-130 sheets, 7 CHAMBRE sheets with a placeholder project name never filled in, 7 CHAMBRE detail sheets mis-stamped with the SDB drawing-number prefix (2 also keep a stale "BANQUETTE" label instead of "TV surround"), an undocumented extra SDB-201 sheet, an unresolved bedroom-cornice "OPTION B" callout, and a bathroom pendant-light quantity still TBC by the lighting designer.
- Discovered a **live SharePoint COC system for K5M**, independent of this repo (`sharepoint_coc_server` connector): `261507_K5M_COC_Fitout.xlsx` and `261307_K5M_COC_FFE.xlsx`, both modified 2026-07-17. The Fitout file's `RFI_Fitout Data` sheet (dated 15.07.2026) appears to match a OneDrive-linked RFI the user shared separately — cross-check still pending, blocked on Microsoft 365 MCP authentication (user needs to run `/mcp`).
- User instructed: stop treating the old `building-COC-Version-00` repo as a reference source at all — it's being deleted. `PROJECT_BRIEFING.md` updated accordingly.
- Open correction pending from user: something in the README's project description ("K5M is a hotel project for Wood Couture... a parallel Fit-out COC is being built") is "not fully correct" — exact fix not yet clarified.
