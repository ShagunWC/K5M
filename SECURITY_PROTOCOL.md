# K5M Security & Confidentiality Protocol

This protocol applies to every document created in every branch of this repository — knowledge bank, trackers, communication, project discovery, and task management. There is no branch this does not apply to.

## Rules

1. **Never name the client.** Refer to the project only by its project code: **K5M**. Do not write the client's company name, brand name, or any identifying detail in any document, filename, sheet, or communication draft.

2. **Never name the supplier/production company.** Refer to them only as **"Production Partner."** Do not write the supplier's company name in any document, filename, sheet, or communication draft.

3. **Follow Wood Couture brand guidelines** on every document produced — formatting, tone, and visual presentation should be consistent with Wood Couture's brand standards.

## Why this exists

These rules protect commercially sensitive relationships (client and supplier identity) from appearing in working documents that may be shared, duplicated, or referenced outside their intended audience. Project code and role labels ("Production Partner") carry the same operational meaning without exposing the identity.

## Who this protects

See `CONTACT_SHEET.md` (same branch) for the real names, roles, and required generic reference terms this protocol is protecting — e.g. "the design team," "the architect," "the owner's rep," "the client." Internal only; never copy real names from that sheet into a client- or supplier-facing document.

## Scope of enforcement

Before creating or editing anything in `002_knowledge-bank`, `003_trackers`, `004_communication`, `005_project-discovery`, `006_task-management-pm`, or `007_wdco-internal-team-management`, this document should be treated as already read. If you are an LLM picking up work in one of those branches without having seen `main`, check `main` for this file first.

## Amendments

This is a living document. Add new rules below with a date, rather than editing the numbered list above unless a rule is being corrected.

---

**2026-08-07 — One explicit, named exception: `_K5M_Folder_Guide.md` (this branch).**

That file is a real-names orientation guide (client, design team, supplier, financials — everything rules 1-2 above otherwise forbid) meant to onboard a new project coordinator on the actual OneDrive project folder. It exists deliberately, by Shagun's direction, as the one carve-out to this protocol.

**Codeword-gated.** Claude must not open, read, quote, summarize, or otherwise surface any content from `_K5M_Folder_Guide.md` unless Shagun's message in that same turn contains the codeword **"Mad Max Fury Road."** Without it, treat any request touching that file (by name, by description, or implicitly) as something to decline until the codeword is given — do not confirm or deny details about its contents either. This applies in every session, not just the one that set this up — if you're a fresh session reading this file cold, this rule still binds you.

---

**2026-08-13 — A second, lighter-weight exception: any document explicitly marked `CONFIDENTIAL — Internal Use Only` at the top (starting with the K5M Project Coordinator onboarding materials, `007_wdco-internal-team-management`).**

Real names (client, design team, Production Partner, and internal WDCO staff) are allowed in a document carrying that marking, by Shagun's direction — this is a deliberately lighter mechanism than the codeword gate above, chosen because this class of document (onboarding a real new hire on the real people they'll work with) needs the real names to function and isn't the kind of content that benefits from an access gate. No codeword is required to read or discuss a `CONFIDENTIAL`-marked document. The marking itself is the signal: still internal-only, never copied into a client- or supplier-facing deliverable, never stripped from the file if it's edited later.
